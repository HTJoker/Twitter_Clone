interface Props {
  path: string;
  pageNumber: number;
  isNext: boolean;
}

const Pagination = ({ path, pageNumber, isNext }: Props) => {
  return <div>Pagination</div>;
};

export default Pagination;
